#!/bin/bash

set -euo pipefail

if [[ "$(id -u)" -ne 0 ]]; then
    printf "%s requires root privileges\n" "$(basename "${0}")"
    exit 1
fi

server_user="hlds"
server_conf="/etc/hlds/cs16-server.conf"
server_workdir="/var/${server_user}"

_is_server_running() {
    pgrep -u "${server_user}" hlds_ &> /dev/null
}

_is_server_updating() {
    pgrep -u "${server_user}" steamcmd &> /dev/null
}

_start_hlds() {
    if [[ ! -f "${server_conf}" ]]; then
        printf "Error: Configuration file %s not found\n" "${server_conf}"
        exit 1
    fi
    eval "$(grep server_params "${server_conf}")"
    runuser -u "${server_user}" -- /bin/bash -c "cd ${server_workdir}/cs16 && exec ./hlds_run -game cstrike ${server_params:-} &"
}

_kill_hlds() {
    timeout_stop=10
    runuser -u "${server_user}" -- /bin/bash -c "pkill -u ${server_user} -SIGINT hlds_"
    for ((i=0; i<timeout_stop; i++)); do
        if ! _is_server_running; then
            return 0
        fi
        sleep 1
    done
    runuser -u "${server_user}" -- /bin/bash -c "pkill -u ${server_user} -SIGTERM hlds_"
    printf "Warning: Server was forcefully terminated\n"
}

_update_hlds() {
    runuser -u "${server_user}" -- /bin/bash -c "cd ${server_workdir}/.steam && exec ./steamcmd.sh +force_install_dir ${server_workdir}/cs16 +login anonymous +app_update 90 validate +quit"
}

start_server() {
    if _is_server_updating; then
        printf "Server is being updated, wait for it to finish\n"
        exit 1
    elif _is_server_running; then
        printf "Server is already running\n"
        exit 1
    fi
    printf "Starting server\n"
    _start_hlds
}

stop_server() {
    if _is_server_updating; then
        printf "Server is being updated, wait for it to finish\n"
        exit 1
    elif ! _is_server_running; then
        printf "Server is not running\n"
        exit 1
    fi
    printf "Stopping server\n"
    _kill_hlds
}

restart_server() {
    if _is_server_updating; then
        printf "Server is being updated, wait for it to finish\n"
        exit 1
    elif _is_server_running; then
        printf "Restarting server\n"
        _kill_hlds
    else
        printf "Starting server\n"
    fi
    _start_hlds
}

update_server() {
    if _is_server_updating; then
        printf "Server is already being updated, wait for it to finish\n"
        exit 1
    elif _is_server_running; then
        printf "Server is running, stop it first\n"
        exit 1
    fi
    printf "Updating server\n"
    _update_hlds
}

status_server() {
    if _is_server_updating; then
        printf "Server is being updated\n"
    elif _is_server_running; then
        pid="$(pgrep -u "${server_user}" hlds_run)"
        uptime_seconds="$(ps -o etimes= -p "${pid}")"
        h="$((uptime_seconds/3600))"
        m="$(((uptime_seconds%3600)/60))"
        s="$((uptime_seconds%60))"
        cmd_line="$(ps -o cmd= -p "${pid}" | cut -d' ' -f 3-)"
        printf "Server is running (PID: %s)\n" "${pid}"
        printf "Uptime: %sh %sm %ss\n" "${h}" "${m}" "${s}"
        printf "Command line: %s\n" "${cmd_line}"
    else
        printf "Server is not running\n"
    fi
}

print_help() {
    printf "Usage: %s <start | stop | restart | update | status>\n" "$(basename "${0}")"
}

case "${1:-}" in
    "start")
        start_server
        ;;
    "stop")
        stop_server
        ;;
    "restart")
        restart_server
        ;;
    "update")
        update_server
        ;;
    "status")
        status_server
        ;;
    "")
        print_help
        ;;
    *)
        printf "Invalid command: %s\n" "${1}"
        print_help
        exit 1
        ;;
esac
